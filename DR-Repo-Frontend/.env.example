# Base URL for the backend API.
# Override this per environment (local dev, staging, production).
# In CI/CD pipelines, set VITE_API_BASE_URL as an environment variable before running `npm run build`.
VITE_API_BASE_URL=http://localhost:5249/api/v2
